# btc_trend_engine runtime — install into a dedicated .venv-engine, NEVER the
# global interpreter (the legacy stack runs on global site-packages and must
# not have numpy/pandas moved under it). Pins match versions verified for
# Python 3.14 in docs/assumptions.md (A5).
#
#   python -m venv .venv-engine
#   .venv-engine\Scripts\pip install -r requirements-engine.txt      (Windows)
#   .venv-engine/bin/pip install -r requirements-engine.txt          (EC2)

fastapi==0.138.1
uvicorn==0.49.0
pydantic==2.13.4
pydantic-settings==2.14.2
websockets==16.0
httpx==0.28.1
SQLAlchemy==2.0.51
alembic==1.18.5
orjson==3.11.9
structlog==26.1.0
numpy==2.4.4
pandas==3.0.2
